# Front-end — React + Vite

Estrutura planejada (ver README na raiz):

```
src/
  main.tsx
  routes/            # vitrine, evento, checkout, meus-ingressos, portaria, ingresso público
  components/        # mapa de assentos, card de evento, scanner de QR
  lib/api.ts         # cliente HTTP + tratamento do formato de erro
  auth/              # contexto de sessão e guarda por papel
  styles/
```
